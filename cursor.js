(() => { if (!window.matchMedia('(pointer: fine)').matches) return; const dot = document.querySelector('.cursor-dot'), ring = document.querySelector('.cursor-ring'); if (!dot || !ring) return; let x = innerWidth / 2, y = innerHeight / 2, ringX = x, ringY = y; addEventListener('pointermove', e => { x = e.clientX; y = e.clientY; dot.style.transform = `translate(${x}px, ${y}px) translate(-50%, -50%)`; }); function animate() { ringX += (x - ringX) * .17; ringY += (y - ringY) * .17; ring.style.transform = `translate(${ringX}px, ${ringY}px) translate(-50%, -50%)`; requestAnimationFrame(animate); } animate(); document.querySelectorAll('a, button, summary').forEach(el => { el.addEventListener('pointerenter', () => ring.classList.add('active')); el.addEventListener('pointerleave', () => ring.classList.remove('active')); }); })();
